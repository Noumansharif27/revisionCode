// let startingNumber = prompt("Enter starting number");
// let endingNumber = prompt("Enter ending number");

// let randomNumber = (start, end) => {
//   let random = Math.floor(Math.random() * end) + start;
//   return alert(random);
// };

// randomNumber(startingNumber, endingNumber);

let string = prompt("Enter your string");

function uniqueChrExtractor(string) {
  let uniqueChar = "";

  for (let i = 0; i < string.length; i++) {
    uniqueChar += i;
    if ((uniqueChar = i)) {
    }
  }
}
